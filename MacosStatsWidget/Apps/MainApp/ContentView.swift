//
//  ContentView.swift
//  MacosStatsWidget
//
//  v0.1 stub — see PLAN.md §4 for the full design.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("macOS Stats Widget — coming soon")
            .padding()
            .frame(minWidth: 360, minHeight: 180)
    }
}
